#include <bits/stdc++.h>

#define ss second
#define ff first
#define mp make_pair

using namespace std;

int qnt[2005], q[2005];
int v[2005];
vector<int> id;
int main(){
	int n, m, x;

	scanf("%d %d", &n, &m);
	for(int i = 0; i < n; i++){
		scanf("%d", &x);
		id.push_back(x);
	}

	for(int i = 0; i < n; i++){
		for(int j = 1; j <= m; j++){
			
		}
	}

	return 0;
}