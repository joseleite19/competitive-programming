#include <bits/stdc++.h>

using namespace std;

map<int, map<vector<int>, int> > dp;

int f(int n, vector<int> v){
	if(dp[n][v] != -1) return dp[n][v];
	if(n == 0){
		bool can = 0;
		for(int i = 0; i < v.size(); i++)
	}
}


int main(){


	for(int i = 0; i < n; i++){
		max(f(i-1, vect), f(i-1, original));
	}








	return 0;
}